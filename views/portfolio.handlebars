<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1" >
		<title>{{ title }}</title>
		{{>mainstyles}}
	</head>
	<body>
		<div id="tdMain">
			{{>topnav}}
			{{>mainheader}}
			{{>carouselmarkup}}
			<div class="footer-spacer"></div>
		</div>
		{{>footer}}
	{{>mainjavascripts}}
	{{>carouselscripts}}
	</body>
</html>