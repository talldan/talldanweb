<!DOCTYPE html>
<html>
	<head>
		<title>{{ title }}</title>
		{{>mainstyles}}
	</head>
	<body>
		<div id="tdMain">
			{{>topnav}}
			{{>mainheader}}
			{{>carouselmarkup}}
			<div class="footer-spacer"></div>
		</div>
		{{>footer}}
	{{>mainjavascripts}}
	{{>carouselscripts}}
	</body>
</html>